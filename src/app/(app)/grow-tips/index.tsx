import { TipsList } from "@/components/grow-tips-list";
import { Layout } from "@/components/layout";

export default function GrowTips() {
  return (
    <Layout title="Dicas para crescer seu salão">
      <TipsList />
    </Layout>
  );
}
